##we will clone the repo 
git clone https://github.com/gopal1409/persistent-terraform-manifest.git
#go inside the repo 
cd persistent-terraform-manifes
###in the code. 
nano ec2.tf
##change the subnet id from the vpc select public subnet
####we will intialized the repo
terraform init
terraform validate
terraform plan
terraform apply 

### terraform apply will always required input as Yes
